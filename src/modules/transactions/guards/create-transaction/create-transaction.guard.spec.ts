import { CreateTransactionGuard } from './create-transaction.guard';

describe('CreateTransactionGuard', () => {
  it('should be defined', () => {
    expect(new CreateTransactionGuard()).toBeDefined();
  });
});
